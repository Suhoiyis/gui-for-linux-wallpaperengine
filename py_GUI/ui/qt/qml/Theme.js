.pragma library

// ── Color tokens ──

// Base palette
var windowBg       = "#1a1b26"
var panelBg        = "#1f2335"
var panelBorder    = "#2f344b"
var textPrimary    = "#c0caf5"
var textSecondary  = "#8a90b8"
var accent         = "#7aa2f7"
var statusBarBg    = "#16161e"

// Card
var cardBg           = "#1f2335"
var cardBorder       = "#2b2f42"
var cardBorderHover  = "#4f5f8f"
var cardBorderActive = "#7aa2f7"
var cardBorderWidth  = 2

// Sidebar
var sidebarBg        = "#1b1f2f"

// Input
var inputBg          = "#16161e"
var inputBorder      = "#2f344b"

// Overlay / divider
var overlayDim       = "#00000030"
var divider          = "#292e42"

// Error / placeholder
var errorBg          = "#374151"
var errorText        = "#9ca3af"

// Semantic accent variants
var typeBlue         = "#93c5fd"
var idOrange         = "#f59e0b"
var sizePink         = "#fda4af"
var favoriteGold     = "#e0af68"
var favoriteGoldBg   = "#665216"
var selectionOverlay = "#7aa2f733"

// Text variants
var textMuted        = "#565f89"
var textBody         = "#a9b1d6"
var textSection      = "#9aa5ce"

// Row alternation (for lists)
var rowEven          = "#1f2335"
var rowOdd           = "#1b1f2f"
var rowSelected      = "#2c3148"

// Apply button (compact mode)
var applyNormal      = "#27a1b9"
var applyHover       = "#7aa2f7"
var applyPressed     = "#3d59a1"

// ── Font tokens ──

var fontSizeXs    = 10
var fontSizeSm    = 11
var fontSizeMd    = 12
var fontSizeLg    = 14
var fontSizeXl    = 16
var fontSize2xl   = 18
var fontSize3xl   = 20

// ── Spacing tokens (4px base grid) ──

var spaceXs   = 4
var spaceSm   = 8
var spaceMd   = 12
var spaceLg   = 16
var spaceXl   = 20
var space2xl  = 24

// ── Sizing tokens ──

var navHeight         = 50
var navButtonSize     = 36
var iconButtonSm      = 28
var iconButtonMd      = 34
var sidebarWidth      = 360
var sidebarPreviewSize = 280

// ── Radius tokens ──

var radiusSm    = 6
var radiusMd    = 8
var radiusLg    = 10
var radiusXl    = 12
var radius2xl   = 14
var radius3xl   = 16
var radiusPill  = 25

// ── Animation tokens (ms) ──

var animFast   = 120
var animNormal = 180
var animSlow   = 420
var animToast  = 150

// ── Timers (ms) ──

var playlistHoverOpen  = 400
var playlistAutoClose  = 300
var toastDuration      = 3000